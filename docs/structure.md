# Estructura del repositorio

> Generado por Guardian Suite (FACTORY-30). Fuente: TLM.

Monorepo with `domain/` (models, ports), `application/` (usecase services), `infra/` (api, helper, driven-adapters/persistence, driven-adapters/ai, driven-adapters/engine), and `iac/`.
